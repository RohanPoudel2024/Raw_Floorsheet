venv/
__pycache__/
*.pyc
.env
.DS_Store

# CSV data tracked via Git LFS
# (data/*.csv are pushed via git lfs — no web UI size limit)

# Old single-file output
floorsheet_data.csv

# Temp files
*.tmp

# Logs (keep locally, don't push)
daily_scraper.log

# Checkpoint (only needed locally)
checkpoint.txt
